// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.gustavohc.interviewme"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
